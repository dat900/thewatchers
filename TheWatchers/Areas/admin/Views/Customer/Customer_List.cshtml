@model IEnumerable<TheWatchers.Models.khachhang>

@{
    ViewBag.Title = "Customer_List";
    Layout = "~/Areas/admin/Views/Shared/_Layout.cshtml";
}

<h2>Customer_List</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tenkh)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.diachi_kh)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.email)
        @*<th></th>*@
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tenkh)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.diachi_kh)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.email)
        </td>
        @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.makh }) |
            @Html.ActionLink("Details", "Details", new { id=item.makh }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.makh })
        </td>*@
    </tr>
}

</table>
